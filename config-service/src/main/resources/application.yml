server:
  port: 8888

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://gitlab.com/hamdiham/ebank-config-repo.git
          clone-on-start: true
          default-label: main
          search-paths: config
